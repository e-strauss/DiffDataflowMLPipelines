/*struct SparseNumVec {
    values: Vec<f64>,
    indices: Vec<usize>,
    size: usize,
    nnz: usize,
}

impl SparseNumVec {
    pub fn new(size: usize, nnz: usize) -> Self {
        SparseNumVec {
            size,
            nnz,
            values: vec![0.0; nnz],
            indices: vec![0; nnz],
        }
    }
}*/